[project]
name = "openrouter-auto"
version = "0.1.0"
description = "OpenAI-compatible proxy for OpenRouter free models with auto-refresh and API key rotation"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.1",
]
